import { Typography } from '@components/typography'
import { StyledLoading } from './styles'

export const Loading = () => (
  <StyledLoading>
    <Typography variant="h3" text="Loading..." />
  </StyledLoading>
)
